@model Electronics.Models.Testimoniale

@{
    ViewData["Title"] = "customerTesti";
    Layout = "~/Views/Shared/_Layout.cshtml";
     var id = ViewBag.session;
}

<h1>customerTesti</h1>


<h4>Testimoniale</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="customerTesti">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TestimonialText" class="control-label"></label>
                <input asp-for="TestimonialText" class="form-control" />
                <span asp-validation-for="TestimonialText" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="UserId" class="control-label"></label>
                <input asp-for="UserId" class ="form-control" value="@id"/>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="customer" asp-controller="Homepagees">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
